# Heisenberg Panel

Private React dashboard for user, administrator, node, host and system operations.

## Development

```bash
npm install
npm run dev
```

Set `VITE_BASE_API` when the API is served from a different origin.

## Production build

```bash
npm run build
```

The static production output is written to `build/`.
