# Marzban `v5.0.0-rc.10`

- همهٔ اصلاح‌های `rc.9` حفظ شده‌اند.
- CI یک build تمیز و مستقل از source اجرا می‌کند.
- image نهایی دقیقاً dashboard build ثبت‌شده در همان commit را حمل می‌کند؛ بنابراین
  تفاوت minifier بین Windows و Linux وارد image سرور نمی‌شود.
- schema، migration، API، query، index و accounting تغییر نکرده‌اند.
