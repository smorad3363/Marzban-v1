# Product-first v3.1 — R03 lifecycle checkpoint

Date: 2026-09-19. Repository: https://github.com/smorad3363/Marzban-v1. Source/branch SHA inspected: `5b2d05a6583f5df1b2e74e1861e630c88c0b235e` on `docs/product-first-v3-1-r01-ledger`. This document is the **newer, additive R03 recovery checkpoint** that supersedes the `current_step` and `next_exact_action` values in the preceding R02 ledger at [`reseller-billing-roadmap.md`, pinned R02 revision](https://github.com/smorad3363/Marzban-v1/blob/5b2d05a6583f5df1b2e74e1861e630c88c0b235e/docs/exec-plans/active/reseller-billing-roadmap.md). Preserve all other earlier register IDs, provenance, decisions, release rules and R00–R02 evidence from that pinned ledger and the user-supplied authoritative `ROADMAP-fa.md`; do not mistakenly restart R03 because the prior ledger is an immutable earlier snapshot.

## Recovery state / one-stage boundary

```yaml
contract_version: product_first_v3.1
branch: docs/product-first-v3-1-r01-ledger
source_sha_inspected: 5b2d05a6583f5df1b2e74e1861e630c88c0b235e
previous_ledger_blob_sha: 15b0b1d4f8f21a8d36e813233e19636252944db5
previous_completed_step: R02
current_step: R03
state: DONE_AUDIT_ONLY
next_exact_action: 'R04: inspect financial debit/refund, usage watermark, rate snapshot, rounding and ledger/transaction boundaries only; no product implementation or release.'
source_code_changed: false
runtime_tests: NOT_RUN
local_worktree_and_diff_check: NOT_AVAILABLE_NO_LOCAL_CLONE
release_gate: LOCKED_IMPLEMENTATION
user_test_confirmation: null
release_actions_performed: []
ci_gate: NOT_VERIFIED_NEW_PRODUCT_IMPLEMENTATION
```

**Explicit user clarification supplied during R03:** The proposed eight fixed-price combinations mean **ONE customer User account allowing 1 or 2 simultaneous device connections**, each for 1/2/3/6 months. `1/2` describes that account's finite `concurrent_user_limit` and associated device slots, **not** one versus two separate User/account rows. The Owner defines eight independent fixed Product prices. Do not multiply these prices again by traffic rate, duration multiplier or Product multiplier. In `USER_CREDIT` the owning Admin is charged/counts **one user-account credit** for this one account regardless of whether it allows 1 or 2 devices. Preserve legacy `SEAT_CREDIT` (weighted device credit) independently; never silently convert it into new Product pricing. Source: user's explicit answer this turn; code correspondence: [`app/utils/admin_billing.py`](https://github.com/smorad3363/Marzban-v1/blob/5b2d05a6583f5df1b2e74e1861e630c88c0b235e/app/utils/admin_billing.py) (`UserCreditStrategy`) and [`app/device_limit/slots.py`](https://github.com/smorad3363/Marzban-v1/blob/5b2d05a6583f5df1b2e74e1861e630c88c0b235e/app/device_limit/slots.py) (`sync_device_slots`). This resolves the R02 open **meaning-of-seats** question as a business decision, while R06 remains PENDING for unit/migration/runtime verification.

## R03.1 Real entry points, state transitions and runtime

| Surface | Verified current path at source SHA | Product-first boundary |
|---|---|---|
| Raw/form user creation | [`app/routers/user.py`](https://github.com/smorad3363/Marzban-v1/blob/5b2d05a6583f5df1b2e74e1861e630c88c0b235e/app/routers/user.py) `POST /api/user`: rejects `USER_CREDIT` and no-form Admin, quotes legacy form rate in traffic modes, delegates `crud.create_user(commit=False)`, applies Access Group, calls `money_billing.charge_form_purchase`, commits, then queues Xray add. [`app/db/crud.py`](https://github.com/smorad3363/Marzban-v1/blob/5b2d05a6583f5df1b2e74e1861e630c88c0b235e/app/db/crud.py) `create_user` validates quotas, namespace, creates User and optional legacy `NextPlan`, synchronizes device slots and records creation. | New usage/allocated/Product paths must not accidentally invoke legacy form/Plan charging or expose unrestricted raw create inputs. Financial details R04. |
| Plan-backed user creation | [`app/routers/admin_hierarchy.py`](https://github.com/smorad3363/Marzban-v1/blob/5b2d05a6583f5df1b2e74e1861e630c88c0b235e/app/routers/admin_hierarchy.py) `POST /api/users/from-plan` -> [`app/utils/admin_plans.py`](https://github.com/smorad3363/Marzban-v1/blob/5b2d05a6583f5df1b2e74e1861e630c88c0b235e/app/utils/admin_plans.py) `create_user_from_plan`: namespace + idempotency replay + Plan/actor scope + required active Access Group; trial quota if trial; PlanVersion determines expiry in days, quota and concurrency; User and UserPlanAssignment + `charge_plan_purchase` transaction; queue Xray add afterward. | Replace commercial entitlement with active/authorized Product in new flows, preserve historical Plan assignment and Trial paths. Creation needs atomic owner/financial/access handling. |
| Edit | `PUT /api/user/{username}` -> `crud.update_user` with locked user and `marzhelp_policy.validate_update`; changes quota, expiry, device count, network and optional legacy `next_plan`; `sync_device_slots`, accounting log, commit, then background Xray update/restart/remove. | New Product usage-mode edits need restricted fields, settle old rate before snapshot change, keep scope and network authority; allocated/seat-mode active period protection tested later. |
| Immediate Plan renewal | `POST /api/users/{username}/renew-from-plan` -> `admin_plans.renew_user_from_plan`: locks User; rechecks replay, scope, Plan archive/trial, limits, Access Group and renewal policy; logs prior usage then **immediately** zeros used bytes, replaces traffic quota/concurrent limit/reset policy, syncs device slots, extends expiry `max(now, expire)+duration_days*86400`, consumes allocated/legacy seat credit where applicable, appends UserPlanAssignment, charges Plan purchase and commits, then queues Xray update. | **NOT a deferred reservation.** Do not treat it as a ready-made pending renewal; new reservation must persist a separate immutable snapshot, one pending per User, charge allocated/seat exactly once at reserve, settle usage at correct boundary, and switch only on first quota/time exhaustion. |
| Legacy next-plan | `crud.create_user` may insert `NextPlan`; `crud.update_user` sets/removes it. `app/jobs/review_users.py` activates via `crud.reset_user_by_next` on quota/time according to legacy `fire_on_either` (otherwise requires both), resets used quota/status/expiry, deletes NextPlan, commits, then Xray update. | Legacy NextPlan lacks demonstrated Product price snapshot/one-pending monetary reservation guarantee. New trigger must be first reached quota OR time; unlimited quota triggers on time only. Never quietly reuse legacy `fire_on_either=false` behavior. |
| Manual and periodic usage reset | `POST /api/user/{username}/reset` -> `crud.reset_user_data_usage`; hourly `app/jobs/reset_user_data_usage.py` performs day/week/month/year resets via same CRUD. CRUD logs UserUsageResetLogs, zeros usage, clears node usages and **deletes legacy next_plan**. `crud.reset_all_users_data_usage` also clears legacy `next_plan` and usage logs. | All reset paths must preserve and/or correctly activate **new** pending periods, not inadvertently erase reservations or cause duplicate charges; verify usage-settlement continuity. |
| Usage collection | `app/jobs/record_usages.py` scheduler reads Xray per-user counters with `reset=True`, aggregates core/node adjusted deltas, updates User usage and Admin usage then calls `money_billing.settle_used_traffic` in same DB transaction, with retry on selected DB errors, afterwards writes optional node stats. | Existing settlement is admin aggregated, not yet verified to meet new per-User Product rate snapshots/watermark and retry/restart guarantees. R04/R22/R23 must trace and test them; no no-double-charge claim now. Xray stat reset precedes DB durability and requires explicit failure/recovery analysis. |
| Delete | `DELETE /api/user/{username}` -> `crud.remove_user` -> `marzhelp_policy.capture_delete`, `db.delete`, commit; then background Xray removal/report/audit. `app/jobs/remove_expired_users.py` every six hours calls `crud.autodelete_expired_users` -> `remove_users` for eligible expired/optional limited accounts. Bulk persistent jobs through `app/routers/bulk.py` and `bulk_operations` mutate Users separately. | Every delete path must finalize usage, resolve/refund pending first and apply mode-specific once-only refund rules before identity removal. Need preserve history and make automatic/bulk paths respect same invariants. No existing new Product refund claim. |
| Runtime/startup | [`app/xray/operations.py`](https://github.com/smorad3363/Marzban-v1/blob/5b2d05a6583f5df1b2e74e1861e630c88c0b235e/app/xray/operations.py) add/update/remove uses device-slot credential emails and Access Group node scopes, with fresh-session `*_by_id` helpers for after-commit tasks; core restart removes established streams. `app/jobs/0_xray_core.py` loads db users on startup and rebuilds disconnected nodes. | Product activation must atomically commit commercial/period state and only then synchronize Xray safely; startup/recovery must not activate or bill again. |

## R03.2 UI and query dependencies

- [`app/dashboard/src/components/CreateUserFromPlan.tsx`](https://github.com/smorad3363/Marzban-v1/blob/5b2d05a6583f5df1b2e74e1861e630c88c0b235e/app/dashboard/src/components/CreateUserFromPlan.tsx): selects Plan + Access Group and posts `/users/from-plan` with idempotency key. Replace for new Product create, do not merely relabel Plan.
- [`app/dashboard/src/components/UsersTablePro.tsx`](https://github.com/smorad3363/Marzban-v1/blob/5b2d05a6583f5df1b2e74e1861e630c88c0b235e/app/dashboard/src/components/UsersTablePro.tsx): legacy renew modal lists `/user-plans` and calls `/users/{username}/renew-from-plan` with key, then immediately refetches User. New reserve/active/cancel UI requires its own explicit state, separate from immediate renew.
- [`app/dashboard/src/components/UserDialog.tsx`](https://github.com/smorad3363/Marzban-v1/blob/5b2d05a6583f5df1b2e74e1861e630c88c0b235e/app/dashboard/src/components/UserDialog.tsx): current raw form edits/creates via DashboardContext, gated Plan-only edit restrictions; when Owner creates User for a different Admin it may create then call `/user/{username}/set-owner` in a **second request**, compensating by DELETE if assignment fails. New financial Product create should avoid split owner/charge transactions, and ensure compensation cannot apply an accidental prepaid refund twice.
- [`app/routers/user_management.py`](https://github.com/smorad3363/Marzban-v1/blob/5b2d05a6583f5df1b2e74e1861e630c88c0b235e/app/routers/user_management.py) currently returns `plan_meta` and `plan_id`/`without_plan` filters, not new Product-period metadata. [`app/dashboard/src/pages/Users.tsx`](https://github.com/smorad3363/Marzban-v1/blob/5b2d05a6583f5df1b2e74e1861e630c88c0b235e/app/dashboard/src/pages/Users.tsx) assembles user dialogs/table/delete/reset/revoke. Preserve unrelated UI behavior.

## R03.3 Evidence/tests, outstanding work and continuation gate

- Existing relevant tests *inspected as code*, not run: [`tests/test_stage4_plan_network_scope.py`](https://github.com/smorad3363/Marzban-v1/blob/5b2d05a6583f5df1b2e74e1861e630c88c0b235e/tests/test_stage4_plan_network_scope.py) covers Plan-backed create requiring Access Group, immediate renewal retaining group/network, historical absent-group fail-closed, node slot scoping and version preservation. Additional existing test files identified for later focused checks include `tests/test_stage3_billing_modes.py`, `tests/test_stage5_restricted_creation_namespace.py`, `tests/test_user_status_integrity.py`, `tests/test_stage11_operations.py`, `tests/test_v115_admin_deletion.py`, `tests/test_user_access_scope.py` and `tests/test_device_limit.py`. Source presence is NOT proof of new Product/one-or-two-device eight-price/reservation correctness. **No tests or local diff check executed in R03.**
- Paths actually inspected show no implemented new Product reservation API or separate durable pending-period service; this is a bounded finding, NOT a claim of exhaustive absence elsewhere. R04 financial audit must verify monetary ledger, snapshot, watermark, rounding and refund separately before implementation. R05 active instruction conflict (`AGENTS.md`, `docs/CODEX/V1_SCOPE.md`) remains unresolved, and release remains LOCKED.
- **Checkpoint write intent:** add this one documentation file only, retaining the previous ledger unchanged as historical checkpoint. After commit, verify file readback, branch head, compare to main (prior ledger + this new doc only), and exact new-SHA CI status. `R03=DONE_AUDIT_ONLY` means source mapping complete, NOT Product behavior implemented, tests passing or readiness to release. **Next stage R04 only** on subsequent explicit continuation. Do not run workflow `Release`, merge, tag, deploy or change `VERSION`.
