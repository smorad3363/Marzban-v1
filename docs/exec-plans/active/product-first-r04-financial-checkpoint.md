# Product-first v3.1 — R04 financial audit checkpoint (revised wallet/debt contract)

Date: 2026-09-19. **Authority:** user-reuploaded `Marzban-Final-Product-v3.1-Revised(1).zip`, SHA-256 `bfbde1cfa3937433ca69735a094b42f825d114c6e1b4cd152a9021d825194886`, authoritative `Marzban-Final-Product-v3.1/ROADMAP-fa.md` sections B05–B20, E/R04. Amendment `wallet_debt_controls_2026-09-19`. The revised ZIP is the *specification*, not an implementation. Initial GitHub source/branch HEAD inspected: `603535e53db999ccb9adc4b8bab27f099683c94b`, branch `docs/product-first-v3-1-r01-ledger`; `main` remained `14e5c9032e5f94f783f34bde391bba318a6de925` at the start. The older ledger (`reseller-billing-roadmap.md`), newer R03 lifecycle checkpoint, and wallet-only decision were fetched and consulted. **This R04 checkpoint supersedes their outdated current/next-stage fields only**, not previous evidence or history.

## Recovery state and hard boundary

```yaml
project: smorad3363/Marzban-v1
contract_version: product_first_v3.1
contract_amendment: wallet_debt_controls_2026-09-19
source_sha_inspected: 603535e53db999ccb9adc4b8bab27f099683c94b
branch: docs/product-first-v3-1-r01-ledger
previous_completed_stage: R03
current_step: R04
state: DONE_AUDIT_ONLY
next_exact_action: 'R05 ONLY: audit Product/Plan/Access Group/Host/Inbound/UI and resolve active AGENTS.md/V1_SCOPE.md instruction conflict before incompatible product-code edits.'
source_code_changed: false
runtime_tests: NOT_RUN_READ_ONLY_AUDIT
local_git_status_diff_check: NOT_AVAILABLE_NO_LOCAL_CHECKOUT
uncommitted_changes_on_other_machines: UNKNOWN
release_gate: LOCKED_IMPLEMENTATION
user_test_confirmation: null
release_actions_performed: []
```

**Single-file write intention:** add only `docs/exec-plans/active/product-first-r04-financial-checkpoint.md` on the existing documentation branch from observed HEAD above; preserve older checkpoint files, all source code and `main`. No product fix, test, CI dispatch, PR, version bump, merge, tag, or release is authorized by this audit. After the write, read back the file, confirm branch HEAD and compare with main, and record exact-SHA check observations separately; do not claim CI PASS on absent checks.

## R04.1 Verified live financial paths versus the revised requirements

1. **Money and legacy parallel capacity (B17).** [`app/db/models.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/db/models.py) `MarzhelpAdminSettings` has `money_balance_toman`, `money_billing_enabled` default false, `used_traffic_price_per_gib_toman`, `usage_billing_remainder`; separately `total_traffic`, `delegated_traffic`, `used_traffic`, `device_capacity_limit`, `capacity_used`, `max_users`. [`app/utils/admin_billing.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/utils/admin_billing.py) differentiates `ALLOCATED_TRAFFIC`, `USED_TRAFFIC`, `USER_CREDIT` (one user-account, no device-weight debit), legacy weighted `SEAT_CREDIT`, `LEGACY_COMPAT`. [`app/utils/marzhelp_policy.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/utils/marzhelp_policy.py) `_validate_traffic_credit` **still increments `settings.used_traffic` and enforces `total_traffic` on allocated create/renew, separately from monetary charge**; `validate_create`, `validate_update`, `validate_next_plan_activation`, `validate_transfer` call this logic. `quota_summary` still calls byte limits `credit_*`. Therefore B17's wallet-only rule is **NOT implemented in new Product paths** (those paths are pending); preserve old balances/migration and avoid second byte-wallet debit in new flows. Per-User quota and independent Admin policy `max_users` must not be conflated with a traffic wallet.
2. **Creation and initial funding (B18).** [`app/routers/admin_hierarchy.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/routers/admin_hierarchy.py) `POST /admin-management/{username}/children` accepts `HierarchyChildCreate.initial_credit`, and if supplied calls **legacy byte** `admin_hierarchy.transfer_credit`, not `money_billing.transfer_money`; [`app/models/admin_hierarchy.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/models/admin_hierarchy.py) `initial_credit` is integer >=1 and offers no `initial_funding_toman` or adjustable `D`. Separate monetary grant/reclaim routes call `money_billing.transfer_money`. Model and inspected monetary migration [`c2f4a8d6e913_add_monetary_reseller_billing.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/db/migrations/versions/c2f4a8d6e913_add_monetary_reseller_billing.py) contain no immutable initial funding `I`, `D`, `H` or debt-status snapshot. **No evidence of automatic first-funding capture, D=floor(5% I), H=floor(15% I), Owner editable D, or I=0 handling.** R09.a–d/R20.a–b and R06 must address a safe additive migration, money transaction ID/actor/time and integer rounding; do not infer I from historical balances or top-ups.
3. **Money grant/reclaim and purchasing (B17–B18).** [`app/utils/money_billing.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/utils/money_billing.py) `transfer_money` checks direct child/actor scope, operation key, locks settings, posts paired `AdminMoneyTransaction` for non-Owner parent, and enforces **balance_after >= 0** on both affected wallets. `charge_plan_purchase` is Plan/version/override-priced, performs a chain of money deltas, locks and rejects any resulting **negative** balance; retries by preexisting key return early but do not compare whole purchase payload. `charge_form_purchase` locks buyer, checks `before >= amount_toman`, uses deterministic `form-money:create:<user_id>` for default create or supplied key, deducts once and records `form_create`; nonpositive prices/Owner bypass this purchase function. `app/routers/admin_hierarchy.py` monetary grant/reclaim performs money mutation + audit and commits in one request. **All three spending entry points require refactoring for a common atomic `balance_after >= -D` guard (never below -H), consistent suspended-mode policy, and full amount/no partial purchase; this is NOT present.** Owner grant/repay/automatic already-paid reservation activation are distinct permissible operations. The existing historical direct-parent reseller-chain transfer/price model must not accidentally change product-first rules without explicit specification.
4. **Traffic and price calculation (B05–B09).** [`app/utils/owner_pricing.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/utils/owner_pricing.py) `get_policy` uses a global Owner `price_per_gib_toman` default 1000 and `OwnerDurationPreset.multiplier_basis_points`; `form_price` multiplies **byte quota × global price × basis points / (2^30 × 10000)** and uses ceiling to whole Toman. It infers days from expiry by rounded seconds/86400 with ±300 seconds tolerance; `adjustment_price` uses ceiling and duration/added-volume legacy adjustments. This is **not** B05's per-Admin base × Product multiplier × duration multiplier or the eight independent Product-keyed fixed Toman prices. [`money_billing.settle_used_traffic`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/utils/money_billing.py) instead uses each **current mutable Admin setting** `used_traffic_price_per_gib_toman`, integer `divmod(remainder + bytes * price, 2^30)` (floor plus carry). These are different existing rounding paths; R06 must document deterministic new rounding/snapshot, not guess. `AdminUserPlanVersion.price_toman` and per-child `AdminUserPlanPrice` are legacy Plan prices, not Product snapshots.
5. **Usage producer, transaction/retry and ledger (B07/B14/R22–23).** [`app/jobs/record_usages.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/jobs/record_usages.py) calls Xray `api.get_users_stats(reset=True)`, aggregates per-User node bytes with coefficients, reads a User→Admin map in a **separate session**, then atomically updates `User.used_traffic`, `Admin.users_usage`, calls `money_billing.settle_used_traffic(db, dict(admin_usage))`, and commits; it retries some deadlocks/integrity failures inside the DB session. Since Xray counters were reset **before durable DB commit**, crash/unrecoverable DB failure can lose a batch, and unpersisted/replayed samples cannot be identified by a durable per-user watermark. Admin totals passed to settlement lose user ID, Product/period/rate attribution. Settlement cascades up a contiguous USED_TRAFFIC parent chain, calculates mutable current Admin rate, and writes **mutable per-Admin/hour** `AdminMoneyTransaction` buckets (`usage:YYYYMMDDHH`) with accumulated `details.usage_bytes` and `gross_charge_toman`. The model's unique `(operation_key, admin_id)` prevents duplicate hourly rows, but it is **not proof of idempotent per-User sample settlement or snapshot pricing**. No per-User/period watermark, settled bytes, price-snapshot ID or immutable per-charge record appears in the inspected models/producer. User deletion or ownership/rate changes between independent sampling/session steps require later explicit race tests. Post-commit `restart_all_cores()` happens for the IDs signaled by settlement. Actual restart/device disconnect outcomes are not synchronously acknowledged by this worker.
6. **Current negative balances, alerts and emergency (B18–B20).** Usage settlement applies unrestricted negative `balance_after` (no D/H check), then flags ACTIVE admins whose balance `<= 0`, calls `admin_hierarchy.suspend_admin(... include_subtree=True, reason_id=2, commit=False)` and restarts all cores after commit. This **contradicts the revised grace period** and risks suspending fully prepaid users: new behavior must allow 0 through -D for permitted operations, block new spending at -D, keep prior usage debt recorded, apply login-only admin emergency freeze at -H, terminate new USED_TRAFFIC consumption as feasibly as sampling/runtime permits, while **not** automatically terminating fully paid ALLOCATED/USER_CREDIT user periods. The periodic reset=True sampling cannot guarantee an exact real-time byte cutoff: report any unavoidable late usage as actual ledger debt and incident, never discard readings. The inspected `MarzhelpAdminSettings`/migration have no initial-funding-based wallet alert state; [`app/utils/dashboard_metrics.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/utils/dashboard_metrics.py) current attention summaries concern users' traffic/expiry/device penalties and Owner admin ACTIVE/SUSPENDED/DISABLED counts, not a B19 monetary threshold event with rearming. Legacy `admin_traffic_warning_percent`/`sudo_traffic_warning_percent` are quota-percentage metrics, **not** 90% of immutable `I`. [`app/routers/admin_hierarchy.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/routers/admin_hierarchy.py) **does already expose** Owner `/admin-management/{username}/freeze` and `/unfreeze` endpoints with operation keys, audit, and background global restart; [`app/db/models.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/db/models.py) has `AdminSuspensionEvent/User/Admin` state. These existing endpoints are a reuse candidate **only after R21.a–b verifies per-node result/partial failure, original-state/eligibility restore and independence from automatic admin-only emergency freeze**. Existing route presence is not evidence of full B20 implementation; no financial linkage or per-node acknowledgement was verified during this R04.
7. **Refund/delete (B11–B12/B14).** [`app/utils/billing_service.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/utils/billing_service.py) `create_refund_request` snapshots an **amount in remaining traffic bytes**, `UserPlanAssignment` and Plan metadata, with unique request/event keys. `decide_refund_request(APPROVED)` changes **`MarzhelpAdminSettings.used_traffic` bytes** and adds `AdminCreditTransfer(resource='allocated_refund')`; it does **not** return Toman to `money_balance_toman`, record price paid or implement automatic <1 GiB paid-period refund. [`marzhelp_policy.capture_delete`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/utils/marzhelp_policy.py) records lifetime bytes, returns refund `0`, with no explicit final USED_TRAFFIC settlement or pending-period money cancellation here. Historical `calculate_delete_refund` also returns 0. Existing Plan `UserPlanAssignment` and `AdminMoneyTransaction` lack Product-period monetary snapshots needed for new cancel/delete. R30–R36 must implement separate once-only pending refund and paid active-period refund based on actual recorded Toman paid, strict active-period used bytes `< 2^30`, while settling outstanding usage and preserving nonrefundable legacy history.
8. **Ledger/schema and integrity (B14/B15).** [`app/db/models.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/app/db/models.py) `AdminMoneyTransaction` records `(operation_key, admin_id)` unique, mode not a dedicated column, actor, counterpart, signed Toman delta and balance before/after, nullable Plan/version/user IDs and JSON details, timestamp. There are **no demonstrated dedicated Product ID/name snapshot, period/reservation ID, effective-rate & multiplier snapshots, durable sample watermark or I/D/H decision fields**. The inspected migration is additive and preserves existing historical wallet/Plan tables. A new additive schema, durable frozen-state/audit/threshold outbox and release-safe migration are **future requirements**, not authorized by the read-only audit. No live database inspection was possible and no empty-database claim is made.

## R04.2 Existing test inventory and precise follow-ups

- **Source inspected, tests not executed:** [`tests/test_stage3_billing_modes.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/tests/test_stage3_billing_modes.py) checks legacy `SEAT_CREDIT` weights, `USER_CREDIT` usage visibility, unused-byte refund approval/one credit event, legacy no-refund delete, price-reseller floor, optional MySQL refund concurrency; the test's refund unit is **bytes**, so its assertions do not establish B11/B12 new Toman refund. [`tests/test_monetary_billing_migration.py`](https://github.com/smorad3363/Marzban-v1/blob/603535e53db999ccb9adc4b8bab27f099683c94b/tests/test_monetary_billing_migration.py) checks additive old migration/model index tokens, not revised B17–B20 fields. Test code presence is not an executed test result.
- R06: confirm whole-Toman 5%/15% floors at tiny I and I=0, duration-to-month precision, GiB=2^30 strict `<1 GiB`, decimal rounding, snapshot time, sampling-overrun bound, owner wallet transfer semantics, whether current live DB contains historic users/credit. Do not silently change active historical billings.
- R09.a–d: first add failing tests for immutable initial funding, debt-guard including parent/child grants, concurrency and payment/replay, new financially relevant schema, Owner editable D only 0..H, 90%/debt/H alerts and admin-only emergency freeze. R09.e–i: independently implement B05–B09 prices and immutable per-Product fixed prices. Revised roadmap's explicit R09.a–i overrides older ledger's provisional R09.a–e split.
- R20.a–d: Admin create initial Toman grant+I capture and Owner edit field/threshold UI; R21.a–b: Owner bulk freeze and selective restore with Node-aware result and retries; R22/R23: persisted user/period delta/rate snapshot, reset-before-commit recovery design, no repeat charge/late-use loss, debt/freeze UX. R25–R36: atomic reserve/refund and delete based on *actual paid Toman*. R42/R43 verify money ledger/thresholds/interrupt/restart/migration against new contract. Preserve self-consistency of one User with 1/2 concurrent device slots and one USER_CREDIT; old SEAT_CREDIT remains distinct.
- **Newly found incompatibility, no new business question:** Existing `create_child.initial_credit` is a separate byte transfer, whereas B18 `I` must be a *Toman first grant*. The user's revised B17–B20 explicitly resolves the desired behavior; this is an implementation/migration gap, not a request to reconfirm the user's decision. Existing `suspend_admin` at wallet <=0 must be replaced/scoped to avoid cutting prepaid clients; avoid repurposing legacy suspension status as login-only without an explicit safe state machine.

**Completion definition:** R04 `DONE_AUDIT_ONLY`: financial source map + contract gaps documented, not any financial code complete. Before next turn, preserve history and inspect exact new GitHub branch SHA. **Next on explicit «ادامه بده»: R05 ONLY**. The repo instruction conflict is still unresolved; no contradictory code changes are permitted until separately addressed. `VERSION`/release/tag/deploy untouched, CI and user acceptance absent.